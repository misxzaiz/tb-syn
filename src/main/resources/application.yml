server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: syn
  redis:
    host: localhost
    port: 6379
    password:
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

logging:
  level:
    org.example: DEBUG
    root: INFO